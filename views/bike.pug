extends base
include _reviewCard

block append head
    //- script(src='/js/mapbox.js')
    

mixin overviewBox(label, text, icon)
    .overview-box__detail
        svg.overview-box__icon
            use(xlink:href=`/img/icons.svg#icon-${icon}`)
        span.overview-box__label= label
        span.overview-box__text= text

block content
    section.section-header
        .header__hero
            .header__hero-overlay &nbsp;
            img.header__hero-img(src=`/img/tours/${bike.imagesCover}`, alt=`${bike.fullName}`)

        .heading-box
            h1.heading-primary
                span= `${bike.fullName}`

            .heading-box__group
                .heading-box__detail
                    svg.heading-box__icon
                        use(xlink:href="/img/icons.svg#icon-anchor")
                    span.heading-box__text= `${bike.weight}kg`

                .heading-box__detail
                    svg.heading-box__icon
                        use(xlink:href="/img/icons.svg#icon-tag")
                    span.heading-box__text= bike.brand

    section.section-description
        .overview-box
            div
                .overview-box__group
                    h2.heading-secondary.ma-bt-lg Quick facts

                    +overviewBox('Category', bike.category, 'grid')
                    +overviewBox('Size',`${bike.size}`, 'maximize-2')
                    +overviewBox('Color',`${bike.color}`, 'flag')
                    +overviewBox('Weight',`${bike.weight}kg`, 'anchor')
                    +overviewBox('Rating',`${bike.ratingsAverage} / 5`, 'star')

        .description-box
            h2.heading-secondary.ma-bt-lg= `About the ${bike.fullName} bike`
            - const paragraphs = bike.description ? bike.description.split('\n') : []
            each p in paragraphs
                p.description__text= p


    section.section-pictures
        each img, i in bike.images
            .picture-box
                img.picture-box__img(
                    src=`/img/tours/${img}`
                    alt=`The Park Camper Tour ${i + 1}`
                    class=`picture-box__img--${i + 1}`
                )

    section.section-reviews
        .reviews
            each review in bike.reviews
                +reviewCard(review)
            
        section.review-form

            form.form.form--review(data-bike-id=`${bike.id}`)
                .form__group
                    label.form__label(for="review") Your review
                    textarea.form__input(
                        id="review"
                        name="review"
                        placeholder="Write your review..."
                        required
                    )

                .form__group
                    label.form__label(for="rating") Rating
                    select.form__input(id="rating" name="rating" required)
                        option(value="5") 5 stars
                        option(value="4") 4 stars
                        option(value="3") 3 stars
                        option(value="2") 2 stars
                        option(value="1") 1 star

                button.btn.btn--green#submit-review(type="submit") Submit review

    section.section-cta
        .cta
            .cta__img.cta__img--logo
                img(src="/img/logo-white.png", alt="Natours logo")

            img.cta__img.cta__img--1(src=`/img/tours/${bike.images[1]}`, alt="Bike picture")
            img.cta__img.cta__img--2(src=`/img/tours/${bike.images[2]}`, alt="Bike picture")

            .cta__content
                h2.heading-secondary What are you waiting for?
                p.cta__text= `The ${bike.fullName} give you infinite memories. Make it yours today!`

                
                if user
                    button.btn.btn--green.span-all-rows#buy-bike(data-bike-id=`${bike.id}`) Buy the bike now!
                else
                    a.button.btn.btn--green.span-all-rows(href='/login') buy this bike

